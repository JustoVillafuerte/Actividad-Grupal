
pregunta 1

paquete com.miempresa.mavenproject1,
public class NewMain {
    
    private static void intercambia(int x, int y)
    {
        int aux;
        aux = x;
        x = aux;
        y = aux;
    }
    
pregunta 2

    private static int max(int x, int y)
    {
        int result;
        if (x >= y)
            result = x;
        else
            result = y;
        return result;
    }
}
