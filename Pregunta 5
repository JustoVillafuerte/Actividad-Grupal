private static int aSaber(int v[], int n)
    {
        int i; int result;
        result=0;
        i=n-1;
        while(i>=0)
        {
            result=result+v[i];
            i=i-1;
        }
        return result;
        
    }
