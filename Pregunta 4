private static int aSaber(int v[], int n)
    {
        int i; int result;
        result = 0;
        i = 0;
        while(i<n)
        {
            result=result+v[i];
            i=i+1;
        }
        return result;
    }
